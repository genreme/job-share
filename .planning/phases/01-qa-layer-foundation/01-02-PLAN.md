---
phase: 01-qa-layer-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - mcp-server/src/tools/jobs.test.js
  - mcp-server/src/tools/updates.test.js
  - mcp-server/src/data/loader.test.js
  - test/dashboard.test.js
autonomous: true

must_haves:
  truths:
    - "MCP tool functions are tested with mocked file system"
    - "Job filtering, sorting, and stats calculation are verified"
    - "Update operations (archive, update, add note) are tested"
    - "Dashboard rendering logic produces correct HTML structure"
  artifacts:
    - path: "mcp-server/src/tools/jobs.test.js"
      provides: "Tests for job query functions"
      min_lines: 100
    - path: "mcp-server/src/tools/updates.test.js"
      provides: "Tests for job update functions"
      min_lines: 80
    - path: "mcp-server/src/data/loader.test.js"
      provides: "Tests for data loading with mocked fs"
      min_lines: 50
    - path: "test/dashboard.test.js"
      provides: "DOM tests for dashboard rendering"
      min_lines: 60
  key_links:
    - from: "mcp-server/src/tools/jobs.test.js"
      to: "mcp-server/src/tools/jobs.js"
      via: "imports and tests exported functions"
      pattern: "import.*from.*jobs\\.js"
    - from: "mcp-server/src/tools/updates.test.js"
      to: "mcp-server/src/tools/updates.js"
      via: "imports and tests exported functions"
      pattern: "import.*from.*updates\\.js"
---

<objective>
Create functional tests for MCP tools (jobs, updates, loader) and visual tests for dashboard rendering.

Purpose: Verify core operations work correctly before adding new features
Output: Test suites covering MCP tools and dashboard rendering
</objective>

<execution_context>
@/Users/genre/.claude/get-shit-done/workflows/execute-plan.md
@/Users/genre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-qa-layer-foundation/01-01-SUMMARY.md

Source files to test:
@mcp-server/src/tools/jobs.js
@mcp-server/src/tools/updates.js
@mcp-server/src/data/loader.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MCP jobs tool tests</name>
  <files>
    mcp-server/src/tools/jobs.test.js
  </files>
  <action>
Create mcp-server/src/tools/jobs.test.js with comprehensive tests:

Mock the loader module to avoid file system dependencies:
```javascript
import { vi, describe, it, expect, beforeEach } from 'vitest'

vi.mock('../data/loader.js', () => ({
  loadJobsFromDashboard: vi.fn()
}))

import { loadJobsFromDashboard } from '../data/loader.js'
import { getJobs, getJobDetail, getJobsByCompany, getApplicationStats, findSimilarJobs, getSearchHistory } from './jobs.js'
```

Test getJobs():
- Returns empty array when no jobs exist
- Filters by status correctly (apply-now, applied, etc.)
- Filters by minFitScore correctly
- Sorts by fitScore descending
- Respects maxResults limit
- Returns correct summary fields (id, title, company, etc.)

Test getJobDetail():
- Returns full job details for valid ID
- Returns error object for non-existent ID
- Calculates daysSinceFound correctly
- Calculates daysSinceApplied correctly
- Includes updateCount

Test getJobsByCompany():
- Finds jobs by exact company name (case insensitive)
- Finds jobs by partial company name match
- Returns empty array when no match

Test getApplicationStats():
- Calculates total correctly
- Groups by status correctly
- Groups by industry correctly
- Calculates fit score distribution (high/medium/low)
- Calculates averageFitScore
- Calculates response rate and interview rate

Test findSimilarJobs():
- Returns error for non-existent job ID
- Finds jobs with same industry
- Finds jobs with similar title keywords
- Ranks by similarity score
- Excludes the source job from results

Test getSearchHistory():
- Returns search history array
- Returns empty array when no history
  </action>
  <verify>
Run `npm test -- --run mcp-server/src/tools/jobs.test.js` - all tests pass.
  </verify>
  <done>
All job query functions tested, mocking file system correctly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create MCP updates tool tests</name>
  <files>
    mcp-server/src/tools/updates.test.js
    mcp-server/src/data/loader.test.js
  </files>
  <action>
Create mcp-server/src/tools/updates.test.js with tests for update operations:

Mock fs and os modules:
```javascript
import { vi, describe, it, expect, beforeEach } from 'vitest'
import * as fs from 'fs'
import * as os from 'os'

vi.mock('fs')
vi.mock('os', () => ({
  tmpdir: vi.fn(() => '/tmp')
}))
```

Test updateJob():
- Updates job fields correctly
- Returns changedFields array
- Adds update entry to job history
- Returns error for non-existent job ID
- Returns "no changes" when values are same
- Increments version on write

Test archiveJob():
- Changes status to archived
- Returns previousStatus
- Adds archive entry to updates
- Returns "already archived" if already archived
- Returns error for non-existent job ID

Test archiveJobs() (bulk):
- Archives multiple jobs
- Returns count and results
- Handles non-existent IDs gracefully
- Handles already-archived jobs

Test setHiringManager():
- Sets hiringManager object
- Adds to connections array
- Adds update entry
- Doesn't duplicate existing connection

Test addJobNote():
- Adds note to updates array
- Sets date and type correctly
- Returns totalUpdates count

Test bulkUpdateJobs():
- Updates multiple jobs
- Tracks changed fields per job
- Returns success count

Create mcp-server/src/data/loader.test.js with tests for loader:
- loadJobsFromDashboard returns data when file exists
- loadJobsFromDashboard returns empty structure when file missing
- loadJobsFromDashboard handles JSON parse errors
- Mock file system for all cases
  </action>
  <verify>
Run `npm test -- --run mcp-server/src/tools/updates.test.js` - all tests pass.
Run `npm test -- --run mcp-server/src/data/loader.test.js` - all tests pass.
  </verify>
  <done>
All update operations tested with proper file system mocking.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create dashboard rendering tests</name>
  <files>
    test/dashboard.test.js
  </files>
  <action>
Create test/dashboard.test.js with DOM tests using happy-dom:

Add environment directive at top of file:
```javascript
// @vitest-environment happy-dom
import { describe, it, expect, beforeEach } from 'vitest'
```

Test job row rendering:
- Creates row with correct status class (status-apply-now, status-applied, etc.)
- Displays job title and company
- Shows fit score with correct color coding
- Shows symbols (emoji) if present
- Shows connection indicator if hasConnections

Test stats display:
- Updates total jobs count
- Updates status breakdown
- Updates fit score distribution

Test filter behavior (logic only, not events):
- Status filter function returns correct subset
- Search filter function matches title/company
- Sort function orders correctly

Test edge cases:
- Empty jobs array renders empty state
- Very long title/company truncated appropriately
- Special characters in job data escaped properly

Note: These tests focus on rendering LOGIC extracted from index.html. The actual dashboard is a monolithic HTML file - test the functions that would be extracted, simulating what they do.
  </action>
  <verify>
Run `npm test -- --run test/dashboard.test.js` - all tests pass.
Run `npm test -- --run` - all test files pass.
  </verify>
  <done>
Dashboard rendering logic verified, DOM tests pass with happy-dom.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `npm test -- --run` runs all tests successfully
2. MCP tool functions are thoroughly tested with mocked dependencies
3. Dashboard rendering logic tests pass with happy-dom
4. No file system side effects during tests (all mocked)
</verification>

<success_criteria>
- MCP jobs.js functions tested: getJobs, getJobDetail, getJobsByCompany, getApplicationStats, findSimilarJobs, getSearchHistory
- MCP updates.js functions tested: updateJob, archiveJob, archiveJobs, setHiringManager, addJobNote, bulkUpdateJobs
- Data loader tested with mocked file system
- Dashboard rendering logic tested with happy-dom
- All tests pass consistently
</success_criteria>

<output>
After completion, create `.planning/phases/01-qa-layer-foundation/01-02-SUMMARY.md`
</output>
